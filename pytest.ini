[pytest]
# The Django packages live under backend/, not at the repo root.
pythonpath = backend
DJANGO_SETTINGS_MODULE = config.settings.dev
python_files = test_*.py tests.py
# Keep the test database between runs — recreating the schema each time dominates
# the run as modules are added. Use --create-db after a migration changes.
addopts = --reuse-db
